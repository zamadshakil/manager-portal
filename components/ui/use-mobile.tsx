// Re-export only — the canonical implementation lives in `hooks/use-mobile.ts`.
// Kept here for backward compatibility with any consumer that still imports
// from `@/components/ui/use-mobile`.
export { useIsMobile } from "@/hooks/use-mobile"
