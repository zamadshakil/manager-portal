# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env*.local
.local.env
local.env

# Common ignores
node_modules
.next/
.DS_Store
tsconfig.tsbuildinfo

# Large binary assets
eng.traineddata

# Migration artifacts — never commit credential scripts or DB dumps
*.backup
*_dump.*
fix_ownership.js
restore_pass.js
update_pass.js
inngest_res.txt
old_schema.sql

# Scratch / one-off debug scripts (not part of the build)
scratch/

# Duplicate lockfile (we use pnpm) — guard against accidental npm/bun installs
package-lock.json
bun.lockb
yarn.lock
